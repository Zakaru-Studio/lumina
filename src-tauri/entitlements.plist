<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Entitlements applied to the macOS bundle. WebKit's JavaScript engine (WKWebView,
    which renders the Tauri UI) needs a writable+executable JIT region; under the
    hardened runtime that requires these entitlements, otherwise the web content
    process is killed at launch ("web content process terminated") and the window
    never appears. `disable-library-validation` lets the app load the WebKit
    frameworks when the bundle isn't Developer-ID signed.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
