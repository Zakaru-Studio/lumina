# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Release signing secrets — NEVER commit. Holds the updater private-key path
# and password used by scripts/release.mjs. See README "Releasing".
.env.release
*.key

# Plain-named local secret env files (updater signing password, SSH deploy key,
# update-host credentials). Distinct from the dotted .env.release above. NEVER commit.
env.release
env.download

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Claude Code — local, machine-specific settings (shared config is committed)
.claude/settings.local.json

# Ad-hoc debug / repro scratch files
repro.html
release-dist/
